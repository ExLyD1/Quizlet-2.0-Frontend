import type { Config } from 'tailwindcss';

const config: Config = {
    content: ['./src/**/*.{html,ts,vue}'],
    theme: { extend: {} },
    plugins: [],
    darkMode: 'class',
};

export default config;
